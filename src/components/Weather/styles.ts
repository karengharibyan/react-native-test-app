import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({
    root: {
        // alignItems: 'center',
        borderWidth: 1,
        padding: 16,
        marginRight: 10
    },
    dateTime: {
        marginBottom: 10
    }
})